struct SomeStruct {
    arr: [i64; 3]
}

fn asdf(xd: [i64; 3]) [i64; 3] {
    xd
}

fn main() {
    let a = [1, 2, 3] i64;
    let mut i = 0;

    printf("[");
    while i < 3 {
        if i == 2 {
            printf("%d", a[i]);
        } else {
            printf("%d, ", a[i]);
        }
        i = i + 1;
    }
    printf("]\n");

    printf("[%d, %d, %d]\n", a[0], a[1], a[2]);
}
