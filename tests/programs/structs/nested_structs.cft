struct InnerStruct {
    field: i64
}

struct OuterStruct {
    inner: InnerStruct
}

fn extract_field(outer: OuterStruct) i64 {
    outer.inner.field
}

fn main() i64 {
    let outer = OuterStruct { 
        inner: InnerStruct {
            field: 42,
        },
    };

    extract_field(outer)
}
