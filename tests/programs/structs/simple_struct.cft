struct SomeStruct {
    first: i64
    second: i64
}

fn add_fields(a: SomeStruct) i64 {
    a.first + a.second
}

fn main() {
    printf("%d",
        add_fields(SomeStruct { first: 5, second: 3, })
    );
}
